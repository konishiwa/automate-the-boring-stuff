# Which of the following are operators, and which are values? 

# * operator
# 'hello' value
# -88.8 value
# - operator
# / operator
# + operator
# 5 value

# Which of the following is a variable, and which is a string?
# spam variable
# 'spam' string

# Name three data types.
# string, float, integer

# What is an expression made up of? What do all expressions do?
#Expressions consist of values (such as 2) and operators (such as +), 
# and they can always evaluate (that is, reduce) down to a single value.

# What is the difference between an expression and a statement?
# An assignment statement consists of a variable name, an equal 
# sign (called the assignment operator), and the value to be stored. 
# expression manipulate the variable with an operator 
# statements set the variable to a value

# What does the variable bacon contain after the following code runs?
# bacon = 20
# bacon + 1
# 21

# What should the following two expressions evaluate to?
# 'spam' + 'spamspam'
# 'spam' * 3
# spamspamspam
# spamspamspam

# Why is eggs a valid variable name while 100 is invalid?
# 100 is a value and cannot be a variable

# What three functions can be used to get the integer, 
# floating-point number, or string version of a value?
# int(), float(), str()

# Why does this expression cause an error? How can you fix it?
# 'I have eaten ' + 99 + ' burritos.'
# Error: trying to concatenate a string and an int. Change the int to a string
print('I have eaten ' + str(99) + ' burritos.')

# Extra Credit: look up what the round() function does
# round function rounds to the amount of digits 
print(round(2.5,1))
print(round(-3.353, 3))
print(round(2.5980, 1))
print(round(3.3))
